[uwsgi]
base_dir       =    ./
domain         =    rproxy
extension      =    wsgi
socket         =    /tmp/%(domain)/%(domain).sock
pidfile        =    /tmp/%(domain)/%(domain).pid
daemonize      =    /tmp/%(domain)/%(domain).log
file           =    %(domain).%(extension)
chmod-socket   =    666